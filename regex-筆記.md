# Regex 正規表達式筆記

> 一行符號，描述「什麼樣的字串才合格」。驗證 email、電話、密碼……一行搞定，取代一堆 `if` / 迴圈 / `Contains`。

[TOC]

---

## 一、C# 怎麼用

最上面要 import：

```csharp
using System.Text.RegularExpressions;
```

### 方法 A：靜態（不用 new，適合少量檢查）

```csharp
bool ok = Regex.IsMatch(input, pattern);   // 回傳 true / false
```

### 方法 B：建立物件（同一規則要重複用很多次時，比較快）

```csharp
Regex re = new Regex(pattern);   // 規則只「編譯一次」
bool ok = re.IsMatch(input);     // 之後重複用這個物件
```

> A 是 `static`（用類別名 `Regex.`，免 new）；B 是建立物件（用 `re.`）。
> 這就是 static vs 非 static 的差別。

### 忘記 using 的後備：寫全名

```csharp
bool ok = System.Text.RegularExpressions.Regex.IsMatch(input, pattern);
```

### 關於 `@"..."`

`@` 是 C# 的「逐字字串」，讓 `\` 直接寫，不用寫成 `\\`。**寫 regex 一律加 `@`。**

```csharp
string p = @"\d";    // 有 @：\d 就是反斜線 d
string p =  "\\d";   // 沒 @：要寫兩條 \\
```

---

## 二、字元家族（要「哪一種字」）

| 簡寫 | 代表 | 包含 |
|------|------|------|
| `\d` | digit 數字 | `0-9` |
| `\w` | word 文字 | 字母 + 數字 + 底線 `_` |
| `\s` | space 空白 | 空格、Tab、換行 |
| `.`  | 任何字元 | 除換行外的任一個字 |

**大寫 = 相反：**

| 簡寫 | 意思 |
|------|------|
| `\D` | 非數字 |
| `\W` | 非文字 |
| `\S` | 非空白 |

> **挑選原則：用「剛好夠、最窄」的那個。**
> 電話是純數字 → 用 `\d`，別用範圍更大的 `\w`（`\w` 連字母都放行）。

---

## 三、數量詞（要「幾個」）

| 寫法 | 意思 | 範例 |
|------|------|------|
| `+`     | 1 個以上 | `\d+` |
| `*`     | 0 個以上 | `\w*` |
| `?`     | 0 或 1 個 | `@?` |
| `{n}`   | 剛好 n 個 | `\d{8}` |
| `{n,}`  | 至少 n 個 | `\w{2,}` |
| `{n,m}` | n 到 m 個 | `\d{2,5}` |

> **核心公式：字元家族 × 數量詞**
> 例：`\d{8}` = 8 個數字、`\w{2,}` = 至少 2 個文字

---

## 四、錨點（框住範圍）

| 符號 | 意思 |
|------|------|
| `^` | 字串**開頭** |
| `$` | 字串**結尾** |

> `^...$` 框起來 = **整串都要符合**，不能有多餘的東西。
> ⚠️ 沒寫 `^ $` 時，`IsMatch` 只要「部分符合」就回 `true`。

---

## 五、字元集合 `[ ]`

| 寫法 | 意思 |
|------|------|
| `[abc]`    | 要是 a / b / c 其中一個（**白名單**）|
| `[^abc]`   | **不能**是 a / b / c（**黑名單**）|
| `[A-Z]`    | 一個大寫字母（`-` 代表範圍）|
| `[a-z0-9]` | 一個小寫字母或數字 |

> ⚠️ `[ ]` 預設是「**要是**這些」；只有在 `[` 後第一個位置加 `^`，才變成「**不能是**」。

---

## 六、跳脫：特殊字元加 `\` 變字面

這些是**特殊符號**，想要「字面上的它」要加 `\`：

```
.  ^  $  *  +  ?  (  )  [  ]  {  }  |  \
```

| 想要的字 | 怎麼寫 | 為什麼 |
|----------|--------|--------|
| 真的點 `.` | `\.` | `.` 本來是「任何字」 |
| 真的加號 `+` | `\+` | `+` 本來是數量詞 |
| `@` | `@` | 普通字元，直接寫 |
| 字母、數字 | 直接寫 | 普通字元 |

> `\` 有兩種角色：
> - `\.` → 把「特殊」變「普通」
> - `\d` → 把「普通字母」變「特殊家族」

---

## 七、實戰範例

| 用途 | Regex | 拆解 |
|------|-------|------|
| Email | `^[^@\s]+@[^@\s]+\.[^@\s]+$` | 非@非空白 + `@` + 非@非空白 + `.` + 非@非空白 |
| 台灣手機 | `^09\d{8}$` | `09` + 8 個數字 |
| 手機含 `-` | `^09\d{2}-\d{3}-\d{3}$` | `0912-345-678` |
| 身分證 | `^[A-Z]\d{9}$` | 1 大寫字母 + 9 數字 |
| 郵遞區號 | `^\d{3,5}$` | 3 到 5 個數字 |
| 市話 | `^0\d{1,2}-\d{6,8}$` | 區碼 + `-` + 號碼 |

範例程式：

```csharp
using System.Text.RegularExpressions;

static bool IsPhone(string text)
{
    string pattern = @"^09\d{8}$";
    return Regex.IsMatch(text, pattern);
}
```

---

## 八、常見陷阱

1. **`^` 有兩個意思**：在 `[]` 外 = 開頭；在 `[]` 裡第一個 = 排除。**位置決定意思。**
2. **點要跳脫**：`.` 要寫成 `\.` 才是真的點，不然代表「任何字」。
3. **`\d` vs `\w`**：`\d` 只數字；`\w` 連字母、底線。純數字（電話）用 `\d`。
4. **記得錨點**：要「整串符合」一定加 `^ ... $`，不然 `IsMatch` 只比「部分」。
5. **`[]` 預設是白名單**：沒加 `^` 是「要是」，加 `^` 才是「不能是」，意思相反。

---

## 九、速查表（考前掃一眼）

```
── 字元家族 ──
\d 數字   \w 字母數字底線   \s 空白   . 任何字
\D 非數字 \W 非文字         \S 非空白

── 數量詞 ──
+  1 個以上      *  0 個以上      ?  0 或 1 個
{n} 剛好 n      {n,} 至少 n      {n,m} n 到 m

── 錨點 ──
^ 開頭    $ 結尾    （^...$ = 整串符合）

── 集合 ──
[abc] 要是其中一個   [^abc] 不能是   [A-Z] 範圍

── 跳脫 ──
特殊字 . ^ $ * + ? ( ) [ ] { } | \  → 要字面意思加 \

── C# ──
using System.Text.RegularExpressions;
Regex.IsMatch(input, pattern)   // → true / false
pattern 用 @"..." 包起來
```

---

## 十、自我測驗（蓋住答案試試）

| 題目 | 答案 |
|------|------|
| 剛好 5 個數字 | `^\d{5}$` |
| 至少 1 個英文字母 | `[A-Za-z]+` |
| 3 個數字 + `-` + 4 個數字 | `^\d{3}-\d{4}$` |
| 不含空白的任意字串 | `^\S+$` |
| 1 大寫字母開頭，後面接數字 | `^[A-Z]\d+$` |
